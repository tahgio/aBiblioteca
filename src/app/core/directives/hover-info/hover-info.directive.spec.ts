import { HoverInfoDirective } from './hover-info.directive';

describe('HoverInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverInfoDirective();
    expect(directive).toBeTruthy();
  });
});
